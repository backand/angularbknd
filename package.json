{
  "name": "Backand",
  "private": true,
  "version": "0.2.1",
  "description": "A starter project for Backand",
  "repository": "https://github.com/backand/angularbknd",
  "license": "MIT",
  "devDependencies": {
    "grunt": "~0.4.2",
    "karma": "~0.10",
    "protractor": "~0.20.1",
    "http-server": "^0.6.1",
    "bower": "^1.3.1",
    "shelljs": "^0.2.6",
    "karma-junit-reporter": "^0.2.2",
    "grunt": "latest"
  },
  "scripts": {

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000",

    "pretest": "npm install",

    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update",

    "preprotractor": "npm run update-webdriver",
    "protractor": "protractor test/protractor-conf.js"

  }
}
